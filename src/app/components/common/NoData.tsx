import React from "react";

const NoData = () => {
  return (
    <div className="text-center py-20 text-gray-500 text-lg h-screen">
      Currently no data available.
    </div>
  );
};

export default NoData;
